function money(n){return Number(n||0).toLocaleString('en-PK');}
function doSearch(){const q=document.getElementById('searchInput')?.value.trim();window.location.href=`pages/products.html${q?'?search='+encodeURIComponent(q):''}`;}
function esc(s=''){return String(s).replace(/[&<>'\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','\"':'&quot;'}[c]));}
async function renderProducts(){const grid=document.getElementById('productsGrid'); if(!grid)return; try{const data=await Products.list({limit:8}); const products=data.products||[]; document.getElementById('cnt-products').textContent=data.total||products.length; grid.innerHTML=products.map(p=>`<div class="product-card pro-card"><div class="product-img">${p.image?`<img src="${esc(p.image)}" alt="${esc(p.name)}">`:'<span class="product-emoji">🎧</span>'}<span class="stock-badge">${p.stock>0?'In stock':'Out of stock'}</span></div><div class="product-body"><div class="product-category">${esc(p.category)}</div><div class="product-name">${esc(p.name)}</div><div class="product-price">PKR ${money(p.price)}</div><div class="seller-strip"><span>Sold by</span><strong>My Store</strong><em>Verified Seller</em></div><div class="product-moq">Stock: ${p.stock}</div><a class="btn-inquiry" style="display:block;text-align:center;text-decoration:none" href="pages/products.html">Buy Now</a></div></div>`).join('');}catch(e){grid.innerHTML='<p>Unable to load products. Check backend URL in js/config.js.</p>';}}
document.addEventListener('DOMContentLoaded',renderProducts);
